upstream fastcgi_backend {
   server php_local:9000;
}

server {
   listen 80;
   server_name localhost nginx_local nginx.local magento_local magento.local;
   set $MAGE_ROOT /var/www/html;
   include /etc/nginx/include.d/include.conf;
}
